using UnityEngine;
using System.Collections;

public class Main : MonoBehaviour {
    public GameObject StarFighter;

	// Use this for initialization
	void Start () {
        Instantiate(StarFighter);
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
